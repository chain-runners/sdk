query getBioForRunner($runnerId: Int!) {
    bioForRunner(runnerId: $runnerId) {
        textContent {
            text
            inlineStyleRanges {
                length
                style
                offset
            }
        }
    }
}
